import { ArrowLeft, BookOpenCheck, RotateCcw } from 'lucide-react'
import type { ArticleReviewPack as ArticleReviewPackData } from '../../features/reader/reviewPack'
import type { Word } from '../../features/words/types'

export function ArticleReviewPack({ pack, savedWords = [], onBack, onOpenReview }: { pack: ArticleReviewPackData; savedWords?: Word[]; onBack: () => void; onOpenReview: () => void }) {
  return <div className="articleReviewPack pageEnter"><button className="textButton articleReviewBack" type="button" onClick={onBack}><ArrowLeft size={16}/> 返回文章</button><section className="articleReviewHero"><div className="readerCompletionIcon"><BookOpenCheck size={23}/></div><span className="eyebrow">ARTICLE REVIEW</span><h1>{pack.title}</h1><p>本文复习包已保存 {savedWords.length} 个词，并保留 {pack.prompts.length} 个最小复习提示。</p><div className="articleReviewMeta"><span>{pack.progress}% 已完成</span>{pack.elapsedSeconds !== undefined && <span>本次用时 {pack.elapsedSeconds} 秒</span>}</div></section><section className="articleReviewSection"><div className="sectionHeading"><div><span className="eyebrow">REVIEW PROMPTS</span><h2>回到本文的表达</h2></div><span>先回忆，再打开词汇复习</span></div><ol className="articleReviewPrompts">{pack.prompts.map(prompt => <li key={prompt.id}><span>{prompt.type === 'comprehension' ? '理解' : '填空'}</span><p>{prompt.prompt}</p></li>)}</ol></section>{savedWords.length > 0 && <section className="articleReviewSection"><div className="sectionHeading"><div><span className="eyebrow">SAVED WORDS</span><h2>本文已保存词</h2></div><span>{savedWords.length} 个</span></div><div className="articleReviewWords">{savedWords.map(word => <span key={word.id}>{word.en}</span>)}</div><button className="secondaryButton" type="button" onClick={onOpenReview}><RotateCcw size={16}/>进入词汇复习</button></section>}</div>
}
