name = "frontend_worker"
description = "Scoped frontend worker for the Lexora application source."
model = "gpt-5.6-luna"
model_reasoning_effort = "max"
sandbox_mode = "workspace-write"
developer_instructions = """
只能在 F:/codex项目/Lexora/src/ 目录内工作，严格遵守指定范围。
独立执行分配的前端任务，遵循现有 React、TypeScript 与样式约定；不做范围外改动。
可行时运行与任务相关的验证，并返回结果、相关文件路径、验证情况及注意事项的简洁汇总。
"""
